// ! NOT READY
function LeapYear(year1, year2) {
  let years = "";
  // return ((year % 4 == 0) && (year % 100 != 0)) || (year % 400 == 0);
  for (year1; year1 < year2; year1 + 4) {
    years += years1;
  }
  console.log(years);
}
